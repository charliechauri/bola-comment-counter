const selectors = {
  section: '.counter',
  indicator: '.counter__indicator',
  increaseButton: '.counter__button--increase',
  decreaseButton: '.counter__button--decrease',
};

module.exports = selectors;
